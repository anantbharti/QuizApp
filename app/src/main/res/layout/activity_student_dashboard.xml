<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".StudentDashboard">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Available Tests"
        android:textColor="#000000"
        android:textStyle="bold"
        android:textSize="22sp"
        android:textAlignment="center"
        android:layout_margin="20dp"
        android:id="@+id/available_tests_heading"
        android:layout_alignParentTop="true"
        android:gravity="center_horizontal" />

    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="3dp"
        android:layout_below="@id/available_tests_heading"
        android:layout_above="@+id/stu_log_out"
        android:id="@+id/recycler_view_at" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="100dp"
            android:layout_marginVertical="30dp"
            android:id="@+id/stu_log_out"
            android:text="Log out"
            android:layout_alignParentBottom="true"
            android:onClick="studentLogOut"/>


</RelativeLayout>